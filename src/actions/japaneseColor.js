import { createAction } from 'redux-actions'

export const fetchColor = createAction('fetch japanese color')
export const openColorModal = createAction('open color modal')
export const closeColorModal = createAction('close color modal')
